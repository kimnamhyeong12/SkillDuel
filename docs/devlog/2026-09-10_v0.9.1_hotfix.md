# v0.9.1 Hotfix

## 수정 1 - WAV Parse Error
`main.gd`의 `.wav` preload 구문을 제거했다.
사운드는 `_ready()`에서 `AudioStreamWAV.load_from_file()`로 직접 불러온다.

## 수정 2 - enemy.gd Parse Error
AI 이동 로직에서 깨진 들여쓰기를 정상화했다.
캐릭터별 이동 성향 보정도 최종 목표 위치에 정상 적용되도록 정리했다.
